# Test-only dependencies — not installed in the production image (see Dockerfile,
# which only installs requirements.txt).
-r requirements.txt
pytest==9.1.1
# tests/test_pat_and_mcp_server.py imports this directly. It currently arrives
# transitively via mcp's own httpx2 dependency, but declaring it here means the
# test file doesn't silently break with ImportError if mcp ever drops/renames it
# or gets swapped for a different MCP client lib — issue #412.
httpx2==2.12.0
